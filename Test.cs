using UIFramework;
using UnityEngine;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
        UIFramework.Manager.Instance.OpenFrame("MainMenu");

	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
